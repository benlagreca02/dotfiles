#!/bin/sh
# Gets and returns the current screen temp 

# gammastep outpus to stderr???
# Thanks Prof. Repka for the 2>&1 trick!
# temp=$(gammastep -p 2>&1 | grep "temperature" | cut -d: -f 3)
# echo "${temp# }/6500K"
hyprctl hyprsunset temperature

